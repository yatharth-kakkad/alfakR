% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction_functions.R
\name{run_transition_karyotype_abm}
\alias{run_transition_karyotype_abm}
\title{Run transition-karyotype treatment ABM}
\usage{
run_transition_karyotype_abm(
  node_metadata,
  edges,
  horizon_timestep,
  transition_scores,
  transition_top_n,
  tau1 = 30,
  p_missegregation = 0.02,
  abm_pop_size = NULL,
  base_death_rate = 0.01,
  base_birth_rate = 0.02,
  fitness_birth_scale = 0.1,
  second_treatment_strength = 0.9,
  abm_delta_t = 1,
  abm_record_interval = 1,
  abm_seed = -1,
  undirected_edges = TRUE,
  untreated_peak_percentile = 99,
  missing_fitness_value = 0
)
}
\arguments{
\item{node_metadata}{Data frame, or path to a CSV, with either the canonical
columns \code{karyotype}, \code{untreated_fitness}, \code{treated_fitness},
\code{is_untreated_peak}, \code{transition_rank}, or the report-export columns
\code{name}, \code{untreated_mean}, \code{untreated_pct_rank}, \code{treated_mean},
\code{treated_pct_rank}.}

\item{edges}{Data frame, or path to a CSV, with from/to columns describing
the karyotype graph.}

\item{horizon_timestep}{Non-negative simulation horizon, aligned to
\code{abm_delta_t}; the ABM runs from step 0 up to this time and records every
\code{abm_record_interval} steps.}

\item{transition_scores}{Data frame, or path to a CSV, with either the
canonical columns \code{karyotype}/\code{transition_score}, or the report-export
columns \code{Transition_Karyotypes}/\code{n}; the top \code{transition_top_n} targets
are selected from this file's ranking.}

\item{transition_top_n}{Number of highest-ranked transition karyotypes to target.}

\item{tau1}{First-treatment time. The treated fitness landscape applies at
\code{t >= tau1}; for example, \code{tau1 = 30} starts treatment on step 30 when
\code{abm_delta_t = 1}.}

\item{p_missegregation}{Per-step probability of graph-neighbor movement.}

\item{abm_pop_size}{Optional initial total population allocated across untreated
peaks. The default \code{NULL} follows the report ABM and places one cell at each
untreated peak.}

\item{base_death_rate}{Baseline per-step death probability.}

\item{base_birth_rate}{Baseline per-step birth probability.}

\item{fitness_birth_scale}{Scaling from active ALFA-K fitness to birth probability.}

\item{second_treatment_strength}{Extra death probability for targeted transition karyotypes.}

\item{abm_delta_t}{Duration of one ABM step.}

\item{abm_record_interval}{Record every N steps.}

\item{abm_seed}{RNG seed, or -1 for random.}

\item{undirected_edges}{Treat edges as undirected.}

\item{missing_fitness_value}{Value used when a node is absent from one
condition-specific ALFA-K landscape and therefore has missing fitness in
that condition. Default is \code{0}, meaning baseline birth only.}
}
\value{
A list with condition-specific count records, metrics, tau2, and endpoint summary.
}
\description{
Implements the report ABM update order: death, graph-neighbor
missegregation, birth, first-treatment fitness switching, automatic second
treatment timing, and endpoint population/diversity reduction summaries.
}
